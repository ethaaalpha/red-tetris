{
  "name": "red-tetris",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build": "bun --filter=client run build && bun --filter=server run build",
    "prod": "bun --filter=server run prod",
    "coverage": "bun --filter=server run coverage"
  }
}
