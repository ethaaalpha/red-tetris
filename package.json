{
  "name": "red-tetris",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build": "bun --filter=client run build && bun --filter=server run build",
    "server": "bun --filter=server run server"
  }
}
